
function [J_matrix] = jacobian(values, ang, mod)
    
    n_ang = length(ang);
    n_mod = length(mod);

    M_1 = zeros(length(n_ang));
    
    
    M_2 = zeros(length(n_ang), length(n_mod));
    M_3 = zeros(length(n_mod), length(n_ang));
    M_4 = zeros(length(n_mod));
    % M_i are sub-matrix of the jacobian

        
    
end